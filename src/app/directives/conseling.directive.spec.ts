import { ConselingDirective } from './conseling.directive';

describe('ConselingDirective', () => {
  it('should create an instance', () => {
    const directive = new ConselingDirective();
    expect(directive).toBeTruthy();
  });
});
